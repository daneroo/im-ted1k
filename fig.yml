# just a start... from the fig example page
summarize:
  build: .
  command: python Summarize.py --days 1 --forever
  volumes:
    - .:/src

monitor:
  build: .
  command: monitor.sh
  volumes:
    - .:/src
  